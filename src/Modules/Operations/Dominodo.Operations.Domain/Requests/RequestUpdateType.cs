namespace Dominodo.Operations.Domain.Requests;

// Progress = staff advance · Comment = comment · Evidence = supplied proof · Resolution = final answer.
public enum RequestUpdateType
{
    Progress,
    Comment,
    Evidence,
    Resolution
}
